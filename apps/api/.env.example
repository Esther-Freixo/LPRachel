# API
PORT=3333
NODE_ENV=development
CORS_ORIGIN=http://localhost:5174

# Banco (dev local via docker-compose) — host na porta 5433
DATABASE_URL=postgresql://rf:rf_local_dev@localhost:5433/rachel?schema=public
# Banco de testes (criar database "rachel_test")
DATABASE_URL_TEST=postgresql://rf:rf_local_dev@localhost:5433/rachel_test?schema=public

# Auth (usado no Plano 2)
JWT_SECRET=troque-isto-por-um-segredo-forte-de-32-chars
ADMIN_EMAIL=rachel@exemplo.com
ADMIN_SENHA=troque-isto

# MinIO (usado no Plano 2) — host na porta 9100
MINIO_ENDPOINT=localhost
MINIO_PORT=9100
MINIO_ACCESS_KEY=rf_minio
MINIO_SECRET_KEY=rf_minio_local_dev
MINIO_BUCKET=rachel-midia

# E-mail (Resend) — recuperação de senha
RESEND_API_KEY=
RESEND_FROM=Professora Esther <onboarding@resend.dev>
APP_URL=http://localhost:5173
